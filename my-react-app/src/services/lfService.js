/**
 * Konfiguration und Meta-Daten für die Kategorie "Liquidität & Forderungen".
 */
export const LF_LAYOUT_CONFIG = {
  title: "Liquidität & Forderungen",
};

/**
 * Gibt den Titel für das Layout zurück.
 */
export function getLfLayoutTitle() {
  return LF_LAYOUT_CONFIG.title;
}